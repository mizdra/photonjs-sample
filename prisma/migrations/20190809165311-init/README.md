# Migration `20190809165311-init`

This migration has been generated by mizdra at 8/9/2019, 4:53:11 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `test_database`.`User`(`id` varchar(767) NOT NULL  ,`name` varchar(767) NOT NULL DEFAULT '' ,PRIMARY KEY (`id`));

CREATE TABLE `test_database`.`Preference`(`id` varchar(767) NOT NULL  ,`fontSize` int NOT NULL DEFAULT 0 ,PRIMARY KEY (`id`));

ALTER TABLE `test_database`.`Preference` ADD COLUMN `user` varchar(767)  ,ADD FOREIGN KEY (`user`) REFERENCES `test_database`.`User`(`id`) ON DELETE SET NULL;

CREATE UNIQUE INDEX `User.id._UNIQUE` ON `test_database`.`User`(`id`)

CREATE UNIQUE INDEX `Preference.id._UNIQUE` ON `test_database`.`Preference`(`id`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190809165311-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,20 @@
+datasource db {
+  provider = "mysql"
+  url      = "mysql://docker:docker@localhost:3306/test_database"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id         String     @default(cuid()) @id @unique
+  name       String
+  preference Preference
+}
+
+model Preference {
+  id       String @default(cuid()) @id @unique
+  fontSize Int
+  user     User
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190809165311-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190809165311-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
